.box_item_dette
    ul(ng-repeat="user in users")
        li {{user.id}}
        li {{user.username}}
        li {{user.Montant}}€
        li {{user.usergroup}}
        li Une pint d'embuscade
//table
    thead
        tr
            th Qui
            th Quand
            th Groupe
            th Id
    tbody
        tr(ng-repeat="user in users")
            td {{user.id}}
            td {{user.username}}
            td {{user.Montant}}€
            td {{user.usergroup}}
